/**
 * GlobalStyles — el CSS global se importa en ExternalDeps.jsx
 * desde src/styles/global.css via Vite.
 * Este componente existe solo para compatibilidad con App.jsx.
 */
export default function GlobalStyles() {
  return null;
}
